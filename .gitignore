zenodo/symphonym-v7.zip
zenodo/symphonym-v6.zip
__pycache__/
*.pyc
.idea/

# Per-host secret overrides — loaded by clustering/config.py after the
# tracked .env, so values here take precedence. Never commit credentials.
.env.local
.env.secret

# Large authority source dumps and derived databases
authorities/chgis/*.sql
authorities/chgis/*.db

# PeriodO gazetteer cache (downloaded by authorities/periodo-places.py)
authorities/periodo_gazetteers_cache/

# DGSD source DB (downloaded/built by authorities/dgsd.places)
authorities/dgsd/*.db


# LPF data artifacts (large; generated/staged for ingestion, not source)
*.lpf
authorities/hgis/*.lpf

# GB1900 HITL generated artifacts (manifest + built review HTML; regenerable)
processing/gb1900/hitl/
